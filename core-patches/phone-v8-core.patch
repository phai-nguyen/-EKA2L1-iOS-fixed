diff --git a/src/emu/kernel/src/codedump_collector.cpp b/src/emu/kernel/src/codedump_collector.cpp
index 2ae102f..26e876d 100644
--- a/src/emu/kernel/src/codedump_collector.cpp
+++ b/src/emu/kernel/src/codedump_collector.cpp
@@ -25,6 +25,13 @@
 #include <common/algorithm.h>
 
 namespace eka2l1::kernel {
+    // double_linked_queue_element::alone() is also true for the sole item in a
+    // roundabout queue (both neighbours are the sentinel).  The codedump
+    // collector needs membership, not "alone" semantics: deque() clears both
+    // pointers to nullptr, while a queued item always has non-null links.
+    static bool collector_linked(const common::double_linked_queue_element &link) {
+        return (link.next != nullptr) || (link.previous != nullptr);
+    }
     codedump_collector::codedump_collector() {
         
     }
@@ -35,7 +42,7 @@ namespace eka2l1::kernel {
             info->parent_seg->dump_collected(info);
         }
 
-        if (!in_decease_list_.empty()) {
+        while (!in_decease_list_.empty()) {
             kernel::codeseg *csg = E_LOFF(in_decease_list_.first()->deque(), kernel::codeseg, garbage_link);
             csg->decrease_access_count();
         }
@@ -45,7 +52,7 @@ namespace eka2l1::kernel {
     }
 
     void codedump_collector::add(kernel::codeseg::attached_info &attach_info) {
-        if (!attach_info.garbage_link.alone()) {
+        if (collector_linked(attach_info.garbage_link)) {
             return;
         }
 
@@ -59,7 +66,7 @@ namespace eka2l1::kernel {
     }
 
     void codedump_collector::remove(kernel::codeseg::attached_info &attach_info) {
-        if (attach_info.garbage_link.alone()) {
+        if (!collector_linked(attach_info.garbage_link)) {
             return;
         }
 
@@ -70,7 +77,7 @@ namespace eka2l1::kernel {
     }
 
     void codedump_collector::add(kernel::codeseg *seg) {
-        if (!seg->garbage_link.alone()) {
+        if (collector_linked(seg->garbage_link)) {
             return;
         }
 
@@ -85,7 +92,7 @@ namespace eka2l1::kernel {
     }
 
     void codedump_collector::remove(kernel::codeseg *seg) {
-        if (seg->garbage_link.alone()) {
+        if (!collector_linked(seg->garbage_link)) {
             return;
         }
 
@@ -93,6 +100,9 @@ namespace eka2l1::kernel {
 
         collected_memory_ -= seg->get_used_memory_size();
         in_list_count_--;
+
+        // Balance the reference acquired by add(codeseg*).
+        seg->decrease_access_count();
     }
 
     void codedump_collector::cleanup(std::uint64_t upcoming_memory_size) {
diff --git a/src/emu/kernel/src/codeseg.cpp b/src/emu/kernel/src/codeseg.cpp
index 31bcc5f..f4307bd 100644
--- a/src/emu/kernel/src/codeseg.cpp
+++ b/src/emu/kernel/src/codeseg.cpp
@@ -118,9 +118,10 @@ namespace eka2l1::kernel {
                         // Clear setting flag from previous use
                         att->get()->flags = 0;
 
-                        if (!att->get()->garbage_link.alone()) {
-                            kern->get_codedump_collector().remove(*(att->get()));
-                        }
+                        // Phone Mode V8: collector membership cannot be tested with alone();
+                        // a sole roundabout entry also reports alone(). remove() is a safe no-op
+                        // for an unqueued link.
+                        kern->get_codedump_collector().remove(*(att->get()));
                         
                         if (!att->get()->closing_lib_link.alone()) {
                             att->get()->closing_lib_link.deque();
@@ -253,9 +254,9 @@ namespace eka2l1::kernel {
         LOG_INFO(KERNEL, "{} (UID3=0x{:X}) runtime data: 0x{:x}", name(), uids[2], the_addr_of_data_run);
 
         if (attaches.empty()) {
-            if (!garbage_link.alone()) {
-                kern->get_codedump_collector().remove(this);
-            }
+            // Phone Mode V8: remove() performs a real linked-state check and balances
+            // the collector reference. We already hold our new attach reference here.
+            kern->get_codedump_collector().remove(this);
         }
 
         attaches.emplace_back(std::make_unique<attached_info>(this, new_foe, dt_chunk, code_chunk));
@@ -385,6 +386,9 @@ namespace eka2l1::kernel {
             kern->destroy(info.code_chunk);
         }
 
+        // Phone Mode V8: unlink queued attach info before erasing it.
+        kern->get_codedump_collector().remove(info);
+
         info.closing_lib_link.deque();
         info.process_link.deque();
 
diff --git a/src/emu/services/include/services/fbs/bitmap.h b/src/emu/services/include/services/fbs/bitmap.h
index 7d5d987..16d1b6d 100644
--- a/src/emu/services/include/services/fbs/bitmap.h
+++ b/src/emu/services/include/services/fbs/bitmap.h
@@ -46,6 +46,24 @@ namespace eka2l1::epoc {
 
     static const std::size_t SUPPORTED_REV2_UID_COUNT = sizeof(SUPPORTED_REV2_UIDS) / sizeof(std::uint32_t);
 
+
+    // Phone Mode V8: emulate the CFbsRasterizer path used by S60/Avkon when
+    // BitGDI reads an NVG extended bitmap produced by AknIconSrv.
+    static constexpr std::size_t NVG_RASTERIZE_MAX_BYTES = 4 * 1024 * 1024;
+
+    static inline bool is_nvg_bitmap_rasterizable(const eka2l1::vec2 &size, const display_mode dpm) {
+        if ((size.x <= 0) || (size.y <= 0)) {
+            return false;
+        }
+
+        const int bpp = get_bpp_from_display_mode(dpm);
+        if ((bpp != 8) && (bpp != 16) && (bpp != 24) && (bpp != 32)) {
+            return false;
+        }
+
+        return (get_byte_width(size.x, bpp) * static_cast<std::size_t>(size.y)) <= NVG_RASTERIZE_MAX_BYTES;
+    }
+
     enum bitmap_file_compression {
         bitmap_file_no_compression = 0,
         bitmap_file_byte_rle_compression = 1,
diff --git a/src/emu/services/include/services/fbs/fbs.h b/src/emu/services/include/services/fbs/fbs.h
index 4fd6f12..31d40a1 100644
--- a/src/emu/services/include/services/fbs/fbs.h
+++ b/src/emu/services/include/services/fbs/fbs.h
@@ -415,6 +415,14 @@ namespace eka2l1 {
         fbsbitmap *create_bitmap(fbs_bitmap_data_info &info, const bool alloc_data = true, const bool support_current_display_mod_flag = true,
             const bool support_dirty = true);
 
+
+        /**
+         * Phone Mode V8: rasterize an AknIconSrv NVG extended bitmap in-place.
+         * This stands in for the phone ROM's CFbsRasterizer so guest BitGDI sees
+         * real pixels rather than compressed NVG commands.
+         */
+        bool rasterize_nvg_bitmap(fbsbitmap *bmp);
+
         /**
          * \brief   Free a bitmap object.
          * 
diff --git a/src/emu/services/src/fbs/impls/bitmap.cpp b/src/emu/services/src/fbs/impls/bitmap.cpp
index b183f21..2e4d7d4 100644
--- a/src/emu/services/src/fbs/impls/bitmap.cpp
+++ b/src/emu/services/src/fbs/impls/bitmap.cpp
@@ -36,10 +36,17 @@
 #include <kernel/kernel.h>
 #include <system/epoc.h>
 #include <utils/err.h>
+#include <utils/guest/akn.h>
 #include <vfs/vfs.h>
 
+#include <common/buffer.h>
+#include <loader/nvg.h>
+#include <lunasvg.h>
+
 #include <algorithm>
 #include <cassert>
+#include <cstring>
+#include <vector>
 
 namespace eka2l1 {
     static epoc::bitmap_color get_bitmap_color_type_from_display_mode(const epoc::display_mode bpp) {
@@ -536,6 +543,11 @@ namespace eka2l1 {
 
         bmp = get_clean_bitmap(bmp);
 
+        // Phone Mode V8: a second client is picking up an immutable extended
+        // bitmap. On a real phone CFbsRasterizer turns NVG into pixels here;
+        // without it guest BitGDI copies compressed NVG bytes as pixel data.
+        server<fbs_server>()->rasterize_nvg_bitmap(bmp);
+
         const std::uint32_t handle_ret = obj_table_.add(bmp);
         const std::uint32_t server_handle = bmp->id;
         const std::uint32_t off = server<fbs_server>()->host_ptr_to_guest_shared_offset(bmp->bitmap_);
@@ -742,6 +754,135 @@ namespace eka2l1 {
         return epoc::get_byte_width(size.x, epoc::get_bpp_from_display_mode(bpp)) * size.y;
     }
 
+
+    bool fbs_server::rasterize_nvg_bitmap(fbsbitmap *bmp) {
+        if (!bmp || !bmp->bitmap_ || (bmp->bitmap_->uid_ != epoc::NVG_BITMAP_UID_REV2)) {
+            return false;
+        }
+
+        epoc::bitwise_bitmap *bws = bmp->bitmap_;
+        std::uint8_t *data = reinterpret_cast<std::uint8_t *>(bws->data_pointer(this));
+        if (!data) {
+            return false;
+        }
+
+        const int width = bws->header_.size_pixels.x;
+        const int height = bws->header_.size_pixels.y;
+        const epoc::display_mode dpm = bws->settings_.current_display_mode();
+
+        if (!epoc::is_nvg_bitmap_rasterizable(bws->header_.size_pixels, dpm)) {
+            return false;
+        }
+
+        const std::size_t raster_bytes = calculate_aligned_bitmap_bytes(bws->header_.size_pixels, dpm);
+        const std::uint32_t header_len = bws->header_.header_len;
+        if (bws->header_.bitmap_size < header_len) {
+            return false;
+        }
+
+        const std::uint32_t available = bws->header_.bitmap_size - header_len;
+        if ((raster_bytes == 0) || (available < raster_bytes) || (available < sizeof(utils::akn_icon_header))) {
+            return false;
+        }
+
+        utils::akn_icon_header *icon_header = reinterpret_cast<utils::akn_icon_header *>(data);
+        if ((icon_header->header_size_ < sizeof(utils::akn_icon_header)) || (icon_header->header_size_ >= available)) {
+            LOG_WARN(SERVICE_FBS, "Phone Mode V8 FBS: invalid Akn icon header while rasterizing NVG");
+            return false;
+        }
+
+        const std::uint8_t *nvg_data = data + icon_header->header_size_;
+        const std::uint32_t nvg_size = available - icon_header->header_size_;
+        const bool is_mask = icon_header->is_mask_;
+
+        std::vector<std::uint8_t> rgba(static_cast<std::size_t>(width) * height * 4, 0);
+
+        if (!is_mask && (icon_header->icon_color_ & 0xFFFFFF)) {
+            // Some colour planes are supplied as a forced tint while the paired mask
+            // carries the shape. Match the existing Window Server NVG path.
+            const std::uint32_t colour = static_cast<std::uint32_t>(icon_header->icon_color_);
+            for (std::size_t i = 0; i < rgba.size(); i += 4) {
+                rgba[i + 0] = static_cast<std::uint8_t>((colour >> 16) & 0xFF);
+                rgba[i + 1] = static_cast<std::uint8_t>((colour >> 8) & 0xFF);
+                rgba[i + 2] = static_cast<std::uint8_t>(colour & 0xFF);
+                rgba[i + 3] = 0xFF;
+            }
+        } else {
+            common::ro_buf_stream nvg_in(const_cast<std::uint8_t *>(nvg_data), nvg_size);
+            common::wo_growable_buf_stream svg_out;
+            std::vector<loader::nvg_convert_error_description> errors;
+
+            loader::nvg_options opts;
+            opts.width = width;
+            opts.height = height;
+            opts.aspect_ratio_mode_ = static_cast<loader::nvg_aspect_ratio_mode>(icon_header->aspect_ratio_);
+
+            if (!loader::convert_nvg_to_svg(nvg_in, svg_out, errors, &opts)) {
+                LOG_WARN(SERVICE_FBS, "Phone Mode V8 FBS: NVG to SVG conversion failed");
+                return false;
+            }
+
+            auto doc = lunasvg::Document::loadFromData(svg_out.content());
+            if (!doc) {
+                LOG_WARN(SERVICE_FBS, "Phone Mode V8 FBS: LunaSVG could not parse converted NVG");
+                return false;
+            }
+
+            lunasvg::Bitmap luna(rgba.data(), width, height, width * 4);
+            doc->render(luna, lunasvg::Matrix{ 1, 0, 0, 1, 0, 0 });
+            luna.convertToRGBA();
+        }
+
+        const int bpp = epoc::get_bpp_from_display_mode(dpm);
+        const int byte_width = bws->byte_width_;
+        std::memset(data, 0, raster_bytes);
+
+        for (int y = 0; y < height; y++) {
+            for (int x = 0; x < width; x++) {
+                const std::uint8_t *src = rgba.data() + (static_cast<std::size_t>(y) * width + x) * 4;
+                std::uint8_t *dst = data + static_cast<std::size_t>(y) * byte_width;
+
+                switch (bpp) {
+                case 8:
+                    // For EGray256 masks the rendered coverage belongs in the byte.
+                    dst[x] = epoc::is_display_mode_mono(dpm) ? src[3] : src[0];
+                    break;
+
+                case 16: {
+                    const std::uint16_t px = static_cast<std::uint16_t>(((src[0] >> 3) << 11)
+                        | ((src[1] >> 2) << 5) | (src[2] >> 3));
+                    *reinterpret_cast<std::uint16_t *>(dst + x * 2) = px;
+                    break;
+                }
+
+                case 24:
+                    dst[x * 3 + 0] = src[2];
+                    dst[x * 3 + 1] = src[1];
+                    dst[x * 3 + 2] = src[0];
+                    break;
+
+                case 32:
+                    dst[x * 4 + 0] = src[2];
+                    dst[x * 4 + 1] = src[1];
+                    dst[x * 4 + 2] = src[0];
+                    dst[x * 4 + 3] = epoc::is_display_mode_alpha(dpm) ? src[3] : 0xFF;
+                    break;
+
+                default:
+                    return false;
+                }
+            }
+        }
+
+        bws->header_.bitmap_size = static_cast<std::uint32_t>(raster_bytes) + header_len;
+        bws->header_.compression = epoc::bitmap_file_no_compression;
+        bws->compressed_in_ram_ = false;
+        bws->uid_ = epoc::BITWISE_BITMAP_UID;
+
+        LOG_INFO(SERVICE_FBS, "Phone Mode V8 FBS: rasterized NVG bitmap {}x{} bpp {}", width, height, bpp);
+        return true;
+    }
+
     static std::uint32_t calculate_reserved_each_side(const std::uint32_t height) {
         // Reserve some space in left and right. Observed shows some apps outwrite their
         // available data region, a little bit, hopefully.
@@ -928,6 +1069,15 @@ namespace eka2l1 {
 
         fbs_server *fbss = server<fbs_server>();
 
+        // Phone Mode V8: AknIconSrv initially asks for only enough bytes to hold
+        // compressed NVG. Reserve the larger raster form now so the emulated
+        // CFbsRasterizer can expand it in-place later.
+        const std::uint32_t vector_size = force_size;
+        if ((assign_uid == epoc::NVG_BITMAP_UID_REV2) && epoc::is_nvg_bitmap_rasterizable(specs.size, specs.bpp)) {
+            force_size = common::max(force_size,
+                static_cast<std::uint32_t>(calculate_aligned_bitmap_bytes(specs.size, specs.bpp)));
+        }
+
         fbs_bitmap_data_info info;
         info.size_ = specs.size;
         info.dpm_ = specs.bpp;
@@ -940,6 +1090,12 @@ namespace eka2l1 {
             return;
         }
 
+        if ((force_size > vector_size) && bmp->bitmap_) {
+            if (std::uint8_t *data = reinterpret_cast<std::uint8_t *>(bmp->bitmap_->data_pointer(fbss))) {
+                std::memset(data + vector_size, 0, force_size - vector_size);
+            }
+        }
+
         if (use_bmp_handles_writeback) {
             // For maximum compability, in normal situation even if the app requested special bitmap type through UID
             // We still assign standard bitmap type UID to the bitmap
diff --git a/src/emu/services/src/window/window.cpp b/src/emu/services/src/window/window.cpp
index b5aa6ae..4264151 100644
--- a/src/emu/services/src/window/window.cpp
+++ b/src/emu/services/src/window/window.cpp
@@ -2300,9 +2300,9 @@ namespace eka2l1 {
     std::uint32_t window_server::get_total_window_groups(const int pri, const int scr_num) {
         epoc::screen *scr = get_screen(scr_num);
 
-        if (!scr) {
-            LOG_TRACE(SERVICE_WINDOW, "Screen number {} doesnt exist", scr_num);
-            return false;
+        if (!scr || !scr->root || !scr->root->child) {
+            LOG_TRACE(SERVICE_WINDOW, "Phone Mode V8: screen {} has no window-group tree yet", scr_num);
+            return 0;
         }
 
         bool hit_priority = false;
@@ -2317,8 +2317,8 @@ namespace eka2l1 {
     std::uint32_t window_server::get_window_group_list(std::uint32_t *ids, const std::uint32_t max, const int pri, const int scr_num) {
         epoc::screen *scr = get_screen(scr_num);
 
-        if (!scr) {
-            LOG_TRACE(SERVICE_WINDOW, "Screen number {} doesnt exist", scr_num);
+        if (!scr || !scr->root || !scr->root->child) {
+            LOG_TRACE(SERVICE_WINDOW, "Phone Mode V8: screen {} has no window-group tree yet", scr_num);
             return 0;
         }
 
@@ -2331,9 +2331,9 @@ namespace eka2l1 {
     std::uint32_t window_server::get_window_group_list_and_chain(epoc::window_group_chain_info *infos, const std::uint32_t max, const int pri, const int scr_num) {
         epoc::screen *scr = get_screen(scr_num);
 
-        if (!scr) {
-            LOG_TRACE(SERVICE_WINDOW, "Screen number {} doesnt exist", scr_num);
-            return false;
+        if (!scr || !scr->root || !scr->root->child) {
+            LOG_TRACE(SERVICE_WINDOW, "Phone Mode V8: screen {} has no window-group tree yet", scr_num);
+            return 0;
         }
 
         window_group_tree_moonwalker walker(scr, infos, window_group_tree_moonwalker::FLAGS_GET_CHAIN, pri, max);
